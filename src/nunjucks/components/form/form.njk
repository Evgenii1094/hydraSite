{%- from  'templates/title.njk' import Title -%}

{% macro Form() %}
    <section class="form">
        <div class="form__container container">
            {{ Title(
                title = 'Join hydra',
                titleSecond = 'Let’s Build Your VR Experience',
                svgIcon = false,
                modifier = 'is-center'
            ) }}
            <form action="#">
                <div class="form__half-width">
                    <input type="text" name="name" placeholder="First Name">
                    <input type="text" name="surname" placeholder="Last Name">
                </div>
                <div class="form__half-width">
                    <input type="text" name="email" placeholder="Email">
                    <input type="number" name="phone" placeholder="Phone Number">
                </div>
                <div class="form__full-width">
                    <input type="text" name="subject" placeholder="Subject">
                </div>
                <div class="form__full-width">
                    <textarea name="text" id="comment" cols="30" rows="10" placeholder="Tell Us Something..."></textarea>
                </div>
                <a href="#" class="btn is-large">Send to hydra</a>
            </form>
        </div>
    </section>
{% endmacro %}
